# Documentation de conception

- `Diagrames de PFE/` contient les diagrammes fonctionnels et de données fournis avec le projet initial.
- `Wireframe for PFE/` conserve le wireframe source de référence.

L’application active et connectée à l’API se trouve dans `../frontend/`. Le wireframe n’est pas utilisé au runtime.
